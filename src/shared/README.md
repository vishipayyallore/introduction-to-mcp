# Shared

Reusable utilities and helpers shared across `src/demos/`.

Place here:
- Common MCP client setup helpers
- Shared type definitions or dataclasses
- Logging configuration
- Config loaders

Import from demos using a relative path or by adding `src/` to `PYTHONPATH`.
